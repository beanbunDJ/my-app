import React from 'react'
import { Button } from 'antd-mobile'

export default function Layoutdefine(props) {
  let type = props.type?props.type:'line';
  
  debugger;
  return (
    <div>
      <Button/>
    </div>
  )
}
